# DOM UI overlays

| File | Role |
|------|------|
| `account-dialog.ts` | Title LINK / account link |
| `sound-comment-viewer.ts` | Sound-test comment + cushion URLs (`Ni`) |
| `sound-comment-composer.ts` | Sound-test write form (`Pi`) |

Handlers/state stay in the game loop; these modules own DOM chrome.
